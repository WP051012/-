# 闯红灯预测 — 完整代码说明文档

## 一、项目结构

```
闯红灯预测/
├── pipeline.py                          # 总入口脚本 (annotate/preprocess/train/inference)
├── cloud_run.sh                         # AutoDL云端运行脚本 (消融实验版)
├── configs/default.yaml                 # 全局配置文件
├── requirements.txt                     # Python依赖
├── ablation_update.tar.gz               # 消融实验更新包 (perception_model + run_experiments)
├── oom_fix_v2.tar.gz                    # OOM修复补丁包 v2
├── 代码说明.md                          # 本文档
│
├── src/                                 # ===== 核心模型代码 =====
│   │
│   ├── perception_model.py              # ★★★ 完整串联模型 (本文核心)
│   │   │                               #   + compute_perception_context() 训练专用
│   │
│   ├── detection/                       # ① 目标检测与追踪
│   │   ├── detector.py                  #   YOLOv8检测器 + COCO类别映射(cls=9→交通灯)
│   │   └── tracker.py                   #   ByteTrack多目标追踪 + 轨迹管理器
│   │
│   ├── graph/                           # ② 交通感知图构建
│   │   ├── node_encoder.py              #   空间编码(8维) + 运动编码(6维) + 多类型节点投影
│   │   ├── gat_conv.py                  #   边权重GAT层 + STRR边权重(交叉内积)
│   │   │                               #   + 相对空间编码(STRR风格8维)
│   │   │                               #   + 按边类型的EdgeFeatureEncoder
│   │   ├── perception_graph.py         #   异质交通感知图 + 时序图栈 + 逐层Pedestrian GRU
│   │   │                               #   + 6种边类型输出
│   │   └── scene_graph.py              #   简化的行人/车辆场景图
│   │
│   ├── memory/                          # ③ 感知记忆模块
│   │   ├── perception_memory.py        #   行为/环境(注意力)/交互记忆 → 融合为感知向量c
│   │   └── decay.py                    #   MAGELLAN记忆衰减 + 时间加权衰减 + 衰减控制器
│   │
│   ├── prediction/                      # ④ 感知注入GRU + 变化检测 + FlowChain
│   │   ├── perception_gru.py           #   感知注入GRU (CM-GRU) + 感知上下文编码器
│   │   ├── change_detector.py          #   结构性变化检测 + 概念漂移检测 (GRU门控)
│   │   ├── flow_chain.py               #   FlowChain预测器 + log_prob()/forward() 双接口
│   │   └── flow_chain_official.py      #   FlowChain官方实现 (Transformer + RealNVP)
│   │
│   ├── classification/                  # ⑤ 闯红灯分类
│   │   └── red_light_classifier.py    #   停止线/路口几何 + MC概率估计 + 反事实模拟
│   │
│   ├── evaluation/                      # ⑥ 评估指标
│   │   └── metrics.py                  #   ADE/FDE/NLL + Acc/Prec/Recall/F1/AUC
│   │
│   └── baselines/                       # ⑦ 对比实验Baseline
│       ├── official_wrappers.py        #   官方模型适配器 (Social-LSTM/STGCNN/STRR)
│       ├── baseline_models.py          #   自实现baseline (FlowChainBase + 分类器)
│       ├── ablation.py                 #   消融变体 (旧版, 已被perception_model ablation flag取代)
│       ├── social_lstm_official/       #   官方Social-LSTM仓库 (git clone)
│       └── social_stgcnn_official/     #   官方Social-STGCNN仓库 (git clone)
│
├── utils/
│   └── traffic_light.py                # HSV交通灯颜色识别 + ROI自动发现
│
├── data/
│   ├── dataset.py                      # 轨迹数据集 (TrajectoryDataset)
│   │                                   #   trajectory_only / with_scene 双模式
│   ├── processed/trajectories/         # 预处理后的轨迹数据 (965个目录, ~247万轨迹)
│   └── annotations/                    # 路口标注 (停止线/路口区域)
│
└── scripts/                            # ===== 运行脚本 =====
    ├── preprocess_fast.py              # 快速预处理 (从标签→轨迹)
    ├── annotate_intersection.py        # 交互式路口标注工具
    ├── extract_traffic_lights.py       # 从视频提取交通灯状态 (HSV)
    ├── generate_violation_labels.py    # 生成闯红灯真值标签
    ├── train.py                        # 训练脚本 (分3阶段, 旧版)
    ├── inference.py                    # 端到端推理脚本
    ├── inference_full.py               # (旧版推理)
    ├── run_experiments.py              # ★ 实验运行 (所有对比+消融, 含OOM修复)
    ├── test_oom_fix.py                 # OOM修复验证脚本 (云端测试)
    └── mem_probe.py                    # 内存压力测试脚本 (云端预检)
```

---

## 二、完整Pipeline流程 (perception_model.py)

### 2.0 训练与推理双路径

模型有两条执行路径，共享 ①~⑧ 的感知pipeline：

| 路径 | 方法 | 用途 | 特点 |
|------|------|------|------|
| **训练路径** | `compute_perception_context()` | 训练时计算 loss | 只算感知上下文，不触发 FlowChain 采样 |
| **推理路径** | `forward()` | 评估/推理 | 完整 pipeline，含 FlowChain 自回归采样 |

```
训练路径 (Teacher Forcing):
  场景数据 → ①~⑧ 感知pipeline → flow_condition
                                          ↓
  观测轨迹 + target轨迹 → flow_chain.log_prob() → 精确 NLL loss

推理路径 (Autoregressive):
  场景数据 → ①~⑧ 感知pipeline → flow_condition
                                          ↓
  观测轨迹 → flow_chain.forward() → z~N(0,I) → N条采样轨迹
```

> **为什么分开？** FlowChain 的自回归采样 (`num_samples=10`) 会产生大量计算图，
> 配合 `grad_accum=4` 时导致 CPU 内存 OOM。训练改用 `log_prob()` (teacher forcing)
> 直接计算目标轨迹在流模型下的精确对数似然，数学上等价但内存开销极低。

### 2.1 详细流程

```
每帧场景数据 (检测框+类别+位置+速度+交通灯状态)
  │
  ├── ① 交通感知图 (TrafficPerceptionGraph)
  │   ├─ 多类型节点编码 (行人270维/车辆14维/交通灯4维)
  │   ├─ 6种边类型构建 (Core↔车辆/行人/交通灯/基础设施)
  │   ├─ STRR风格边权重 (交叉内积+sigmoid)
  │   ├─ 按边类型的边特征编码 (每种边类型不同特征,见第四节)
  │   ├─ 2层边权重GAT消息传递
  │   └─ 逐层Pedestrian GRU更新目标节点
  │   → node_embeddings, target_embedding
  │
  ├── ② 感知记忆 (TrafficPerceptionMemory)
  │   ├─ 行为记忆 ← 目标行人特征 (MLP编码)
  │   ├─ 环境记忆 ← 交通灯/基础设施 (目标query注意力加权 + 校准门)
  │   └─ 交互记忆 ← 周围车辆/行人 (自注意力聚合)
  │   → behavioral, environmental, interactive 三个向量
  │
  ├── ③ 记忆融合 (MemoryFusion)
  │   └─ c = LayerNorm(W[c] · [behavioral‖environmental‖interactive])
  │   → 交通感知向量 c (256维)
  │
  ├── ④ 衰减控制器 (DecayController)
  │   ├─ 观测到的记忆 → 置信度增加 (EMA更新)
  │   ├─ 未观测的记忆 → 置信度衰减 (MAGELLAN风格)
  │   └─ 置信度低于阈值 → 记忆清零 (遗忘)
  │
  ├── ⑤ 感知状态变化检测 (PerceptionChangeDetector)
  │   ├─ 结构性检测 (规则驱动):
  │   │   ├─ 节点出现/消失 (新目标进入/离开场景)
  │   │   ├─ 交通灯状态切换 (红↔绿)
  │   │   ├─ 智能体数量显著变化
  │   │   └─ 车道变化
  │   └─ 概念漂移检测 (数据驱动):
  │       └─ 监控GRU门激活统计分布偏移 (KL散度)
  │
  ├── ⑥ 变化响应 (核心机制)
  │   ├─ 结构性变化 → 清除受影响的记忆槽位
  │   ├─ 概念漂移 → 重置全部记忆 + GRU隐状态
  │   └─ → 重新生成感知向量c → FlowChain重预测
  │
  ├── ⑦ 感知上下文编码 (PerceptionContextEncoder)
  │   └─ 时序GRU: [b₁‖e₁‖i₁], [b₂‖e₂‖i₂], ... → c₁, c₂, ..., c_T
  │   → 感知上下文序列 (捕捉感知的时序动态)
  │
  ├── ⑧ CM-GRU编码 (PerceptionGRU)
  │   ├─ 重置门 r ← 环境记忆 (当前规则约束了什么?)
  │   ├─ 更新门 z ← 交互记忆 (周围目标如何影响?)
  │   └─ 候选 n  ← 行为记忆 (目标本身的意图?)
  │   → 感知注入的轨迹隐状态
  │
  └── ⑨ 分流:
        │
        ├── [训练] flow_chain.log_prob(obs, target, perception_c)
        │   └─ target轨迹 → 逆向流过NVP耦合层 → z → log N(z;0,I) + log|det|
        │   → 精确负对数似然 loss (2.74s/条, ~0.03GB GPU)
        │
        └── [推理] flow_chain.forward(obs, perception_c, num_samples=N)
            ├─ Transformer编码观测轨迹 → 历史编码
            ├─ [历史编码‖感知向量c] → 条件融合 (FiLM)
            ├─ z ~ N(0,I) → 4层RealNVP仿射耦合 → 未来轨迹 y
            └─ N次采样 → 轨迹分布
            → {samples, mean, std, log_probs}
            └── (Stage 3) → ⑩ 闯红灯分类 (RedLightProbabilityEstimator)
```

---

## 三、核心模块详解

### 3.1 交通感知图 — `src/graph/perception_graph.py`

**6种边类型** (对应6种节点交互关系):

| 类型 | 关系 | 特征维度 | 额外特征 |
|:----:|------|:------:|------|
| 0 | Core(目标行人) ↔ Vehicle | 5 | 相对距离+方向+速度差+**车辆速度** |
| 1 | Core ↔ Person (其他行人) | 4 | 相对距离+方向+速度差 |
| 2 | Core ↔ Infra (交通灯) | 6 | 相对距离+方向+**红/绿/黄状态** |
| 3 | Vehicle ↔ Vehicle | 4 | 相对距离+方向+速度差 |
| 4 | Infra ↔ Infra | 3 | 相对距离+方向 |
| 5 | Person ↔ Person | 4 | 相对距离+方向+速度差 |

**边权重计算** (对齐STRR官方实现):
```
embed_src = ReLU(W_src · [h_src ‖ spatial_src])
embed_dst = ReLU(W_dst · [h_dst ‖ spatial_dst])
w_ij = σ(embed_src · embed_dst / √d)      ← 交叉内积, 非自内积
```

**相对空间编码** (STRR风格8维):
```
[dxmin, dymin, dxmax, dymax, dxc, dyc, w_obj, h_obj]
```

### 3.2 感知记忆 — `src/memory/perception_memory.py`

| 记忆 | 来源 | 编码方式 | 作用 |
|------|------|---------|------|
| 行为记忆 | 目标行人特征 | MLP | 自身行为意图 |
| 环境记忆 | 交通灯+基础设施特征 | 目标query注意力 + 校准门 | 交通规则约束 |
| 交互记忆 | 周围智能体特征 | 自注意力 (屏蔽自己和基础设施) | 社交交互影响 |

融合: `c = LayerNorm(W · [behavioral‖environmental‖interactive])`

### 3.3 CM-GRU — `src/prediction/perception_gru.py`

```
标准GRU:
  r = σ(W_ir·x + W_hr·h)
  z = σ(W_iz·x + W_hz·h)
  n = tanh(W_in·x + r ⊙ W_hn·h)
  h' = (1-z) ⊙ n + z ⊙ h

CM-GRU (Cognition-Modulated GRU):
  r = σ(W_ir·x + W_hr·h + U_r · environmental)    ← 环境记忆调控信息遗忘
  z = σ(W_iz·x + W_hz·h + U_z · interactive)      ← 交互记忆调控状态更新
  n = tanh(W_in·x + r ⊙ W_hn·h + U_n · behavioral) ← 行为记忆驱动候选状态
```

可解释性: 每个门的激活可以追溯到具体的感知输入
- 重置门高 → 环境约束强, 需要更新内部状态
- 更新门高 → 交互关系变化, 需要保持旧状态
- 候选强 → 行为意图明确, 驱动新状态方向

### 3.4 FlowChain — `src/prediction/flow_chain.py` + `flow_chain_official.py`

**双文件架构**:
- `flow_chain_official.py`: FlowChain 官方实现 (ICCV 2023)
  - `TransformerFlowChain`: Transformer encoder-decoder + RealNVP
  - `LinearMaskedCoupling`: MADE 风格掩码仿射耦合层 (4 blocks, hidden_dim=128)
  - `MeanScaler` / `NOPScaler`: 数据标准化
- `flow_chain.py`: 薄封装层，提供统一接口
  - `FlowChainPredictor`: 包装 TransformerFlowChain，适配 (B,T,2) 格式
  - `log_prob()`: **训练专用** — 精确密度估计 (teacher forcing)
  - `forward()`: **推理专用** — 自回归采样

**架构**:
```
Transformer 编码器:
  input = [obs(2) + pe(16) + cond(256)] → Linear(274, d_model=64)
                                 ↑
                          感知向量c (当前256维, ⚠️ 待压缩到64)

潜变量 z ~ N(0,I)  →  RealNVP流  →  轨迹 y
                         ↑
                 cond → dist_args_proj → 每步Flow条件
```

**仿射耦合层** (FiLM条件):
```
x分为两半: x_a, x_b
y_a = x_a
y_b = x_b ⊙ exp(s(x_a, c)) + t(x_a, c)

其中 scale被clamp到[0.01, 100]防止数值溢出
层间用随机置换打乱维度
```

**两个核心方法**:

| 方法 | 方向 | 输入 | 输出 | 用途 |
|------|------|------|------|------|
| `log_prob(obs, target, perception_c)` | 逆向 | target轨迹 | 标量 log p(target\|obs,c) | 训练 — 算 NLL loss |
| `forward(obs, perception_c, num_samples)` | 正向 | z~N(0,I) | {samples, mean, std, log_probs} | 推理 — 生成轨迹 |

**`log_prob()` 原理** (teacher forcing):
```
target轨迹 y → 逆向流过4层RealNVP耦合层 → 潜变量 z
log p(y|c) = log N(z; 0, I) + Σ log|det(Jacobian)|
loss = -log p(y|c).mean()
```
- 优势：精确似然 + 极低显存（不产生采样计算图）
- 评估阶段仍用 forward() + best-of-100 采样，与 paper 一致

### 3.5 变化检测 — `src/prediction/change_detector.py`

**结构性变化** (规则驱动):
- `node_appear`: 新节点出现 → 清除交互记忆
- `node_disappear`: 节点消失 → 清除交互记忆
- `traffic_light_change`: 红绿灯切换 → 清除环境记忆
- `agent_count_change`: 数量显著变化 → 清除交互记忆

**概念漂移** (数据驱动):
- 监控CM-GRU的reset_gate和update_gate激活值
- 30帧滑动窗口 vs 参考窗口
- 均值偏移超过阈值(15%) → 检测到漂移
- 触发: 清除全部记忆 + 重置GRU → 重预测

### 3.6 记忆衰减 — `src/memory/decay.py`

**MAGELLAN风格机制**:
```
观测到: c_new = min(1.0, c_old + α_learn)
        memory = (1-α) × memory + α × new_vector (EMA)

未观测: c_new = max(0.0, c_old - α_decay)
        c < threshold → memory清零 (遗忘)
```

3个记忆槽各自独立衰减 + 统一衰减控制器

---

## 四、Baseline模型详情

### 4.1 Social-LSTM (`SocialLSTMOfficial`)
- **官方仓库**: `social_lstm_official/model.py` → `SocialModel` 类
- **论文**: Alahi et al., CVPR 2016
- **核心**: LSTM + 网格社交池化
- **输出**: 5维双变量高斯 (μx,μy,σx,σy,ρ)
- **适配方式**: 使用官方LSTM cell (`importlib.util` 从指定文件导入)
  - 单人场景(V=1): 绕过社交池化(`getSocialTensor`), 直接用官方LSTMCell编解码
  - 理由: 社交池化要求多行人场景, 单人场景下社交信息无意义, 不影响公平性
  - 预测: LSTM编码8帧观测→自回归解码12帧未来→取高斯均值(μx,μy)作为输出

### 4.2 Social-STGCNN (`SocialSTGCNNOfficial`)
- **官方仓库**: `social_stgcnn_official/model.py` → `social_stgcnn` 类
- **论文**: Mohamed et al., CVPR 2020
- **核心**: 时空图卷积 (GCN+TCN)
- **输入**: (N,2,T,V) + 邻接矩阵(K,V,V)
- **输出**: (N,5,T_pred,V) 高斯参数
- **适配**: V=1单节点, 单位邻接矩阵, 取前2通道(μx,μy)

### 4.3 STRR (`STRROfficial`)
- **官方代码**: STR-PIP/models/model_graph.py → `GraphModel`
- **论文**: STR-PIP, arXiv 2020
- **核心**: 图关系推理 + 行为预测
- **适配**: 官方依赖ResNet提取图像特征, 我们的数据没有 → 手写等价模型
  - 轨迹空间编码替代ResNet
  - 2层GCN + ped_gru + temporal_gru + 二分类器

### 4.4 FlowChain / OurMethod

- **FlowChain baseline** (`FlowChainBase`): 无条件版本，`perception_c=0` → FlowChainPredictor → TransformerFlowChain。与 OurMethod 共享同一底层预测器，区别仅在于是否接收感知条件。
- **OurMethod** (`TrafficPerceptionModel`): 完整感知版本。`stage=2` 激活感知pipeline（图+记忆+CM-GRU+变化检测），`stage=1` 退化为无条件 FlowChain。
- **消融变体**: 通过 `ablation` 参数控制（不再使用独立的 ablation.py 类），见 5.4 节。

---

## 五、实验配置

### 5.1 数据划分
```
Train: 2026_01_15, 01_21, 01_22, 01_23  (cap到10万)
Val:   2026_01_26                         (cap到1万)
Test:  2026_01_27                         (cap到1万)
```

### 5.2 实验1: 轨迹预测对比

**运行命令**:
```bash
python scripts/run_experiments.py --config configs/default.yaml \
    --processed-dir data/processed/trajectories --label-dir labels \
    --exp trajectory --epochs 10 \
    --skip social-lstm
```
> Social-LSTM 因单行人场景下社交池化无意义，已通过 `--skip social-lstm` 跳过。

**训练参数**:

| 参数 | 值 | 说明 |
|------|-----|------|
| max_agents | 10 | 每场景最多保留10个交通参与者 (OOM修复) |
| grad_accum | 1 | 每样本立即更新 (避免计算图堆积) |
| 训练方式 | teacher forcing | `log_prob()` 精确似然 (OOM修复) |
| 推理方式 | best-of-100 | `forward()` + 100条采样 (与原方案一致) |
| batch_size (scene) | 16 (外层) → 逐样本处理 | DataLoader batch=16, 内部逐条 forward |
| epochs | 10 | ~3小时 (398条/epoch × 10 × ~2.7s/条) |
| 评估采样数 | 100 (best-of-100) | |
| optimizer | AdamW, lr=1e-3, weight_decay=1e-4 | |
| scheduler | CosineAnnealingLR | |

**评估指标** (与原方案一致):

| 模型 | 来源 | ADE ↓ | FDE ↓ | NLL ↓ |
|------|------|:-----:|:-----:|:-----:|
| Social-LSTM | 官方SocialModel + 适配器 | — | — | — |
| Social-STGCNN | 官方social_stgcnn + 适配器 | 56.34 | 68.15 | — |
| FlowChain | 自实现FlowChainBase (c=0) | 36.85 | 44.68 | 148.77 |
| **OurMethod** | TrafficPerceptionModel + scene数据 | **34.92** | **44.62** | 169.07 |

**相对提升**:
| 对比 | ΔADE | ΔFDE | ΔNLL |
|------|:----:|:----:|:----:|
| vs Social-STGCNN | **-38.0%** | **-34.5%** | — |
| vs FlowChain | **-5.2%** | ≈0% | +13.6% ⚠️ |

> **注**: Social-LSTM 因单行人场景下社交池化无意义+训练困难，已放弃。
> NLL 反增分析见第八节待修复 P1。

**与原始方案的一致性**:
- 评估方式未变: 所有模型用 `evaluate_trajectory()` 统一评估，best-of-100 采样
- 指标计算未变: ADE/FDE 用像素空间 L2，取中位数 (与 FlowChain paper 一致)
- 仅训练方式改变: 自回归采样 → teacher forcing (log_prob)，对最终结果有利无害

### 5.3 实验2: 闯红灯分类 (需违规标签)

| 模型 | Acc | Prec | Recall | F1 | AUC |
|------|:--:|:----:|:------:|:--:|:--:|
| LSTM-Classifier | ✓ | ✓ | ✓ | ✓ | ✓ |
| GRU-Classifier | ✓ | ✓ | ✓ | ✓ | ✓ |
| STRR | ✓ | ✓ | ✓ | ✓ | ✓ |
| OurMethod | ✓ | ✓ | ✓ | ✓ | ✓ |

### 5.4 实验3: 消融实验 (2026-07-31 重构)

**设计原则**: 从完整 OurMethod 出发，每次只关掉一个模块（ablation flag），所有变体用相同 scene data + train_perception_model() 训练。

**运行命令**:
```bash
# 单个变体（逐个手动跑，每个 --segment 2 自动分段释放内存）
python scripts/run_experiments.py --exp ablation --variant NoGraph \
    --epochs 10 --segment 2 --resume checkpoints/ablation_nograph.pt
```

**6 个消融变体**:

| 变体 | flag | 关掉什么 | 替换为 | 参数量 | 预估/epoch |
|------|------|---------|--------|:-----:|:--------:|
| **FullModel** | `None` | — | — | 1,999K | ~18 min |
| **NoGraph** | `"no_graph"` | GAT 图推理 | NodeFeatureEncoder 简单编码 | 1,897K | ~15 min |
| **NoMemory** | `"no_memory"` | 三支记忆+上下文编码器 | 直接 Linear 投影 | 1,299K | ~11 min |
| **NoCogContext** | `"no_cogcontext"` | 认知状态c注入GRU | GRU仅含traj+GAT | 2,407K | ~16 min |
| **NoFlowChain** | `"no_flowchain"` | 概率流预测 | 确定性 MLP | 1,511K | ~9 min |
| **NoChange** | `"no_change"` | 变化检测+衰减控制器 | — | 1,999K | ~17 min |

**验证逻辑**:
- NoGraph 差 → 证明 GAT 图推理对理解交通场景有价值
- NoMemory 差 → 证明三支记忆分解是有效归纳偏置
- NoCogContext 差 → 证明认知状态c对GRU时序编码有贡献
- NoFlowChain 差 → 证明概率建模优于确定性预测
- NoChange 差 → 证明动态场景适应机制有效

> **实现**: `TrafficPerceptionModel.__init__(ablation=flag)` 条件化创建/替换子模块，
> `compute_perception_context()` 和 `forward()` 均有对应分支。详见 `src/perception_model.py`。

---

## 六、评估指标

**轨迹预测**:
- ADE: 预测与真值平均L2距离 (越低越好)
- FDE: 预测终点与真值终点L2距离 (越低越好)
- NLL: 负对数似然 (越低越好)。FlowChain-based 模型通过 `sample_with_log_prob()` 逐帧计算并累加。确定性模型 (STGCNN) 不输出 NLL。

**闯红灯分类**:
- Accuracy, Precision, Recall, F1-score, AUC-ROC

---

## 七、当前状态

### ✅ 代码完整性

| 组件 | 文件 | 状态 |
|------|------|:--:|
| YOLO检测 + ByteTrack追踪 | `detector.py`, `tracker.py` | ✅ |
| 交通感知图 (6边类型) | `perception_graph.py` | ✅ |
| 感知记忆 (3种+衰减) | `perception_memory.py`, `decay.py` | ✅ |
| CM-GRU (感知注入) | `perception_gru.py` | ✅ |
| 变化检测 (结构+漂移) | `change_detector.py` | ✅ |
| FlowChain (概率轨迹) | `flow_chain.py`, `flow_chain_official.py` | ✅ |
| 闯红灯分类 (MC概率) | `red_light_classifier.py` | ✅ |
| 完整串联模型 | `perception_model.py` | ✅ |
| Baseline适配器 | `official_wrappers.py` | ✅ |
| 实验框架 | `run_experiments.py` | ✅ |
| OOM修复 | `perception_model.py` + `run_experiments.py` | ✅ |
| 内存压测 | `mem_probe.py` | ✅ |
| 预处理+标注+推理 | `scripts/` | ✅ |

### ✅ 已串联

| 组件 | 状态 |
|------|:--:|
| 感知图 → 记忆 → CM-GRU → 变化检测 → FlowChain | ✅ perception_model.py |
| 变化检测 → 记忆清除 → GRU重置 → 重预测 | ✅ _handle_change_events() |
| 训练路径: compute_perception_context → log_prob (teacher forcing) | ✅ train_perception_model() |
| 推理路径: forward → autoregressive sampling (best-of-N) | ✅ evaluate_trajectory() |
| OurMethod 用 TrafficPerceptionModel + 场景数据 | ✅ run_experiments.py |
| Social-LSTM/STGCNN/FlowChain baseline | ✅ official_wrappers.py |

### ⏳ 实验进度

| 实验 | 状态 | 环境 |
|------|:--:|------|
| 实验1 (Trajectory) — Social-LSTM | ❌ 放弃 | — |
| 实验1 (Trajectory) — Social-STGCNN | ✅ 已完成 | AutoDL RTX 4090D |
| 实验1 (Trajectory) — FlowChain | ✅ 已完成 | AutoDL RTX 4090D |
| 实验1 (Trajectory) — OurMethod | ✅ 已完成 | AutoDL RTX 4090D |
| 实验2 (Classification) | ⏳ 待跑 | 需要违规标签 |
| 实验3 (Ablation) — 6 变体 | 🔄 云端运行中 | AutoDL RTX 4090D × 2 |

### ⚠️ 待修复

| # | 项目 | 说明 |
|---|------|------|
| P1 | FlowChain条件维度压缩 | perception_c 256→64 bottleneck，详见第八节 |
| — | train.py | 仍用旧`RedLightPredictionModel`, 需改用`TrafficPerceptionModel` |
| — | CounterfactualSimulator | 代码已有但未接入任何脚本 |
| — | 实验2 | 需要违规标签 (从视频提取交通灯状态) |
| — | 云端实验2 | 需要原始视频文件 |

---

## 八、关键修复记录

### 07-30: OOM 修复 + 训练策略优化

| 修复项 | 文件 | 说明 |
|--------|------|------|
| compute_perception_context() | `perception_model.py` | 新增训练专用方法，只算感知上下文不触发FlowChain采样 |
| log_prob teacher forcing | `perception_model.py` + `flow_chain.py` | 训练用精确似然替代自回归采样，GPU显存从32MB→32MB但CPU大幅降低 |
| grad_accum 4→1 | `run_experiments.py` | 每样本立即step，释放计算图，避免CPU内存堆积 |
| max_agents 20→10 | `run_experiments.py` | 每场景最多保留10个最近交通参与者，GAT计算量减半 |
| 逐样本处理 | `run_experiments.py` | train_perception_model() 内部逐条 forward，batch_size 仅用于DataLoader |
| OOM catch + skip | `run_experiments.py` | 单个样本OOM时自动跳过，不中断训练 |
| 内存压测脚本 | `scripts/mem_probe.py` | 5步压测: 数据加载→模型构建→多agent数→30步训练→综合判定 |
| 验证脚本 | `scripts/test_oom_fix.py` | 云端快速验证修复是否生效 |

**OOM 根因分析**:
1. **GPU OOM**: 有 scene_data 时 forward() 走自回归采样 (num_samples=10×FlowChain)，计算图爆显存
   - 修复: 训练与推理分离，训练走 log_prob() teacher forcing
2. **CPU OOM**: grad_accum=4 导致4份感知pipeline计算图堆积 (每份含GAT+GRU+注意力)
   - 修复: grad_accum=1 + max_agents=10

**内存实测 (AutoDL RTX 4090D, 80GB CPU)**:
- CPU: 26.6 GB (33%), GPU 峰值: 0.03 GB (<1%)
- 单步 ~2.74s, 398条/epoch × 10 epochs ≈ 3小时

### 07-31: 消融实验重构 + NLL诊断

| 修复/发现 | 文件 | 说明 |
|-----------|------|------|
| ablation flag 机制 | `perception_model.py` | `__init__(ablation=flag)` 条件化创建/替换子模块，6种模式 |
| 消融 pipeline 分支 | `perception_model.py` | `compute_perception_context()` + `forward()` 全面支持ablation分支 |
| 消融实验重写 | `run_experiments.py` | 所有变体用 `TrafficPerceptionModel(stage=2, ablation=flag)` + scene数据 |
| --variant 单变体运行 | `run_experiments.py` | 支持逐个变体独立训练+评估 |
| no_flowchain 训练 | `run_experiments.py` | train_perception_model 新增 MSE loss 路径 |
| **NLL代码审查** | — | 确认NLL计算一致，但condition(256维)在encoder输入中占比128:1过大 |

**NLL 反增根因分析**:
- `flow_chain_official.py:_encode()`: encoder输入 = [obs(2) + pe(16) + cond(256)] → Linear(274, 64)
- 256维 condition 在数值上主导了仅 2 维的轨迹输入
- 仅 ~4000 次梯度更新（398样本×10epoch），模型未学会从 condition 提取有效信号
- **修复方案 (P1)**: 加 `cond_bottleneck: 256→64`，condition_dim 从 256 改为 64

### 07-28: 模型架构修复

| 修复项 | 说明 |
|--------|------|
| cls=9类别映射 | COCO cls=9→traffic_light |
| STRR边权重Bug | 自内积→交叉内积 |
| 相对空间编码 | 新增RelativeSpatialEncoder(8维) |
| Pedestrian GRU | 感知图加逐层ped_gru |
| FiLM Coupling clip | exp(s)限制[0.01,100] |
| flow_chain.condition_dim | 补上缺失属性 |
| ADE/FDE用中位数 | FlowChain采样取中位数 |
| 6种边类型编码 | EdgeFeatureEncoder改为按边类型 |
| 完整串联模型 | 新建TrafficPerceptionModel |
| 变化检测→记忆重置 | _handle_change_events串联 |
| deque→list | change_detector适配GPU .to() |
| importlib导入 | 官方模型从指定文件导入,避免sys.path冲突 |
| context_encoder维度 | 修复PerceptionContextEncoder (T, D)→(B, T, D) |
| forward维度标准化 | 自动补batch dim,统一(B,T,2)格式 |
| 本地全面测试 | forward pass + training loop本地跑通 |
