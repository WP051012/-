# 2026-08-04 代码修改清单

## 本次修改的文件（需上传云端，共 3 个）

---

### 1. `src/perception_model.py` — 健壮性加固 (3处)

**1a. 空节点索引越界防护**
- 新增 `_safe_target_embed()`：node_emb 为空 → zeros；非空 → `min(target_idx, N-1)` 防越界
- `compute_perception_context()` 和 `forward()` 共 4 处索引访问全部改用此方法
- `graph_builder.build()` / `perception_memory()` 的 target_idx 也加了安全处理
- **额外修 bug**：`forward()` no_graph 分支原代码漏写 `target_emb` 提取，会 NameError

**1b. `_change_events` → deque(maxlen=500) 防内存泄漏**
- `list` → `deque`，超容量自动丢弃最旧记录，`extend()`/`clear()` 完全兼容

**1c. `_get_frame_data()` 健壮性增强**
- 维度分派：3分支 → 4分支（新增 dim=2 单帧支持）
- positions 兼容 dim=2/3/4 三种情况
- 新增空节点检测：`b_t.numel()==0` → 返回 None

---

### 2. `src/baselines/baseline_models.py` — SocialSTGCNN 时序解码器修复

**问题**：`x.mean(dim=-1)` 把 TCN 保留的时序结构全抹平，一个 Linear 硬生成 12×2=24 个值

**修复**：
```
旧: TCN → mean(dim=-1) → Linear(H, 24) → reshape → (B, 12, 2)
新: TCN → Conv1d×2 → AdaptiveAvgPool1d(12) → transpose → Linear(H, 2) → (B, 12, 2)
```
新增：`temporal_proj` + `temporal_upsample` + `output_proj`（逐帧解码）
删除：`output_fc`

---

### 3. `src/baselines/official_wrappers.py` — STRR 完整重写 + 实验2配套

**旧版问题**：单节点假图、`ped_embed`/`ctxt_embed`/`ped_gru` 定义了从未调用、`W(curr)` 只是一层 Linear

**新版架构**（按 STR-PIP 论文）：
```
SpatialEncoder(8→128) → GraphBuilder(多节点)
  → Inner-product adjacency: A_ij = σ(φ·θ^T / √d)
  → 2-layer GCN: H' = ReLU(A_norm·H·W) + residual
  → TargetProj(128→512) → PedestrianGRU(2-layer)
  → TemporalGRU(2-layer) → Classifier → logits
```
输入改为 `obs + scene_data`（仅观测轨迹，不用 GT 未来）

---

### 4. `scripts/run_experiments.py` — 实验2 重写

**删除**：LSTM-Classifier / GRU-Classifier baseline + 假 OurMethod

**新实验2 结构**：

| 模型 | 方法 | 训练 |
|------|------|------|
| STRR | GCN+内积边+GRU → BCE 分类器 | `train_classifier_with_scene()` |
| OurMethod | 感知→预测→几何判定 (stage=3) | 加载 stage-2 ckpt → `evaluate_ourmethod_violation()` |

**新增函数**：`train_classifier_with_scene()`、`evaluate_classifier_with_scene()`、`evaluate_ourmethod_violation()`

---

## 上传命令

```bash
# 上传 4 个文件到云端
scp src/perception_model.py root@<ip>:~/red-light-prediction/src/
scp src/baselines/baseline_models.py root@<ip>:~/red-light-prediction/src/baselines/
scp src/baselines/official_wrappers.py root@<ip>:~/red-light-prediction/src/baselines/
scp scripts/run_experiments.py root@<ip>:~/red-light-prediction/scripts/

# 删旧 checkpoint（旧架构不兼容）
ssh root@<ip> "cd ~/red-light-prediction && rm -f checkpoints/ablation_*.pt"
```

## 实验运行命令

```bash
# === 消融实验（3个未训练变体）===
python scripts/run_experiments.py --exp ablation --variant NoCogContext --epochs 10 --segment 2
python scripts/run_experiments.py --exp ablation --variant NoFlowChain --epochs 10 --segment 2
python scripts/run_experiments.py --exp ablation --variant NoChange --epochs 10 --segment 2

# === 实验2: STRR vs OurMethod ===
python scripts/run_experiments.py --exp classification

# === NoMemory 重评估 ===
python scripts/reeval_nomemory.py
```
